interface:
  display_name: "Claude Code Review"
  short_description: "Call local Claude Code for independent code reviews."
  default_prompt: "Use $claude-code-review for Claude Code review / cc审查 of the current project or diff, then summarize the highest-risk findings."
